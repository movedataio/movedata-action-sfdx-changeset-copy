import { FlagsConfig, SfdxCommand } from '@salesforce/command';
import { PackagingSObjects } from '@salesforce/packaging';
type UninstallResult = PackagingSObjects.SubscriberPackageVersionUninstallRequest;
export declare class PackageUninstallCommand extends SfdxCommand {
    static readonly description: string;
    static readonly examples: string[];
    static readonly requiresUsername = true;
    static readonly flagsConfig: FlagsConfig;
    run(): Promise<UninstallResult>;
}
export {};
