import { SfCommand } from '@salesforce/sf-plugins-core';
export type ForceCommandResult = {
    apiVersion: string;
};
export declare class ForceCommand extends SfCommand<ForceCommandResult> {
    static readonly hidden = true;
    static readonly examples: any[];
    static readonly summary = "Display the ASCII art logo for the Salesforce CLI";
    run(): Promise<ForceCommandResult>;
    protected _help(): void;
}
