import { SfCommand } from '@salesforce/sf-plugins-core';
import { Connection, Org } from '@salesforce/core';
export type PasswordData = {
    username?: string;
    password: string;
};
export type GenerateResult = PasswordData | PasswordData[];
export declare abstract class UserPasswordGenerateBaseCommand extends SfCommand<GenerateResult> {
    protected usernames: string[];
    protected passwordData: PasswordData[];
    protected connection: Connection;
    protected org: Org;
    protected length: number;
    protected complexity: number;
    generate(): Promise<GenerateResult>;
    private print;
}
