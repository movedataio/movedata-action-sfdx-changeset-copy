import { ReleaseType } from 'semver';
export type BumpType = Extract<ReleaseType, 'major' | 'minor' | 'patch'>;
export declare const meetsVersionCriteria: (title: string, maxVersionBump: BumpType) => boolean;
export declare const maxVersionBumpFlag: import("@oclif/core/lib/interfaces").OptionFlag<string, import("@oclif/core/lib/interfaces/parser").CustomOptions>;
export declare const getOwnerAndRepo: (ownerFlag: string, repoFlag: string) => Promise<{
    owner: string;
    repo: string;
}>;
