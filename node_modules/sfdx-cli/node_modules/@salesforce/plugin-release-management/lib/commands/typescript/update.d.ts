import { SfCommand } from '@salesforce/sf-plugins-core';
import { Interfaces } from '@oclif/core';
export default class Update extends SfCommand<void> {
    static readonly summary: string;
    static readonly description: string;
    static readonly flags: {
        version: Interfaces.OptionFlag<string, import("@oclif/core/lib/interfaces/parser").CustomOptions>;
        target: Interfaces.OptionFlag<string, import("@oclif/core/lib/interfaces/parser").CustomOptions>;
    };
    private typescriptPkg;
    private repo;
    private packages;
    private flags;
    run(): Promise<void>;
    private getPackages;
    private updateEsTargetConfig;
    private updateTsVersion;
    private determineNextTsVersion;
    private validateEsTarget;
    private validateTsVersion;
}
